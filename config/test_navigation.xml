<?xml version="1.0"?>
<root BTCPP_format="4">
  <BehaviorTree ID="MainTree">
    <!-- <Repeat num_cycles="100"> -->
      <Sequence>
        <RateController hz="0.2">
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="SMAC"/>
        </RateController>
        <FollowPath path="{path}" controller_id="TEB"/>
      </Sequence>
    <!-- </Repeat> -->
  </BehaviorTree>
</root>
